

service: tasks-backend-api
frameworkVersion: '2'

provider:
  name: aws
  runtime: java8
  #lambdaHashingVersion: 20201221
  region: eu-west-2
  profile: serverless

package:
  artifact: target/tasks-api-java-dev.jar

functions:
  get-restaurant-api:
    handler: com.serverless.GetRestaurantHandler

    events:
      - http:
          path: rest/{restId}/task
          method: get

